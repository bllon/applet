<!--index.wxml-->
<view class="container">
  <view class="nav">
    <block wx:for="{{list}}" wx:for-index="index" wx:for-item="item">
      <view class="box" bindtap="jump" data-label="{{item.name}}">{{item.name}}</view>
    </block>
  </view>
</view>
